## MESA-Build Finetune Examples

Standalone, runnable projects that are the canonical demonstrations of finetune flow (and also represent smoke tests).

- `aws/` — SageMaker / `HuggingFaceLoRATrainer` fine-tuning.
- `mlx/` — local `MLXLoRATrainer` fine-tuning (depends on `mesa-finetune[mlx]`).