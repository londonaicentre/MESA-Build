name: llamaserve test

on:
  pull_request:
    branches:
      - main
    paths:
      - infer/local/llamaserve/**

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: infer/local/llamaserve
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.x"
      - run: pip install uv
      - run: uv sync
      - run: uv run pytest tests
