name: genoschema test

on:
  pull_request:
    branches:
      - main
    paths:
      - schemas/genoschema/**

jobs:
  test:
    uses: ./.github/workflows/test.yml
    with:
      working-directory: schemas/genoschema
