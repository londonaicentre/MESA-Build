[project]
name = "llm-assets" 
description = "LLM assets" 
version = "1.0.0"
requires-python = ">=3.12"
[build-system]
requires = ["setuptools>=80.9.0"]
build-backend = "setuptools.build_meta"
[tool.setuptools.package-data]
llm_assets = [
    "templates/*",
    "examples/*"
]
